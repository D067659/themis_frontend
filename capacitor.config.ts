import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.themis.app',
  appName: 'themis_frontend',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
